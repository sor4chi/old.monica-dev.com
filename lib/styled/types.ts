export interface ThemeType {
  variables: {
    headerHeight: string;
  };
  colors: {
    text: string;
    subText: string;
    background: string;
    subBackground: string;
    primary: string;
  };
}
